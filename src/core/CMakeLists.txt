set(KERNEL_MODULE "${PROJECT_NAME}.ko")

execute_process(COMMAND make
        WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
        )

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/${KERNEL_MODULE}
        DESTINATION ${CMAKE_CURRENT_BINARY_DIR})